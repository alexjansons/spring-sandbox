pluginManagement {
	repositories {
		maven { url '/home/lukas/.m2/repository' }
		maven { url 'https://repo.spring.io/milestone' }
		maven { url 'https://repo.spring.io/snapshot' }
		gradlePluginPortal()
	}
}
rootProject.name = 'disconnect-error'
